name: inbodysimpletracker
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^2.30.0
  firebase_auth: ^4.19.0
  cloud_firestore: ^4.17.0
  provider: ^6.1.2
  fl_chart: ^0.69.0
  google_mlkit_text_recognition: ^0.11.0
  cupertino_icons: ^1.0.8
  image_picker: ^1.2.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# 這裡是正確的位置
flutter:
  uses-material-design: true
  assets:
    - assets/images/
